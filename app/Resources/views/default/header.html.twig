<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}Maciej Piątek{% endblock %}</title>
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Oswald" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
    <!-- Bootstrap -->
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}"/>
</head>
<body>
<header>
    <div class="container">
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="collapsed navbar-toggle" data-toggle="collapse"
                            data-target="#bs-example-navbar-collapse-3" aria-expanded="false"><span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span>
                    </button>
                    <a href="{{ path('homepage') }}" class="navbar-brand">MaciejPiatek.pl</a></div>
                    {% include('default/navbar.html.twig') %}
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-3">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <a class="btn btn-primary navbar-btn pull-right" href="{{ path('fos_user_security_logout') }}">
                            {{ 'Logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    {% else %}
                        <a class="btn btn-primary navbar-btn pull-right"
                           href="{{ path('fos_user_security_login') }}">{{ 'Login'|trans({}, 'FOSUserBundle') }}</a>
                    {% endif %}
                </div>
        </nav>
    </div>
</header>
